Full name of this app Randomn P2P Chat

apk release name to be called p2p-random-chat.apk

Description:
Auto Random Peer 2 Peer (p2p) Chat & File Transfer App

This is not the p2p chat and file t6ransfer app, (that expects you to  know each other) this is for random users to auto pair up with other random users using the same app to be able to chat and send files

For use with chat, and file transfere via public turn server to allow chat messaging and file transfer (send & recieve via p2p for ipv4 and ipv6) using qr code or pin code (to pair a connection using a public turn server aswell as connect with random user using the same android apk app

using example for inspiration https://jnetai.com/apps/net-chat/index.html and already included files in this project folder /home/jay/Documents/Scripts/AI/OpenCode/More-Apps/p2p-random-chat/

instead of exit (via back button built in to phone os for example) it should confirm exit not just exit / quit / close app!

project location /home/jay/Documents/Scripts/AI/OpenCode/More-Apps/p2p-random-chat/

apk app location to be stored in /home/jay/Documents/Scripts/AI/OpenCode/More-Apps/p2p-random-chat/apk/

This is not to replace the other p2p app, this is simular and made for random users to chat without requiring a centralized server or requiring user to know the other user beforehand, still show users pair code and share link so user can share if they need to, but have a connect with random user button that searches in a loop forever till it finds another p2p app user (using same app, this app) also show block user (that blocks and searches for the next user to connect / pair with, and skip user (to find another user without blocking but puts them as lowest priortiy only connect back to them if no other user is searchable), once connected / paired then they are no longer in the searchable status untill they skip or block a user)

use github workflows to build the app and put finaly release in apk folder in the project location

Dont edit this prompt file

Never change anything in Backup folders (if it exists) but you can use them as a read-only reference if a mistake is made and you need to fix something

save changes to file(s) in question

then after files are added / edited then save any changes made to changes.txt

Implement persistent error handling and debugging throughout the project but not in a degub version just have the apk file in releases have good debug code to help with any errors or issues. Every failure, exception, or unexpected state should generate a clear error code, detailed debug output, and useful diagnostic information to help identify the exact cause quickly.
Do not remove debugging systems after issues are fixed — keep all error codes, logging, stack traces, validation checks, and diagnostic tools permanently integrated so that any future bugs, crashes, or unexpected behaviour can be traced and resolved efficiently.

always use same keystore for each app update made via github workflows so it can update correctly without requiring uninstallation

Save changes to changes.txt (create if not exists)

Tell me when ready to test (stay quiet after acknowledging you got the message / request / mission every time and stay quiet till its ready to test and respond only if fully complete  or if you need input from me or if I ask for an update)!

when giving final github release link (where applicable), make sure it points to the newest release but without the tag or filename so I can see the correct location without direct downloading the file as thats best practice!

github repo location to be https://github.com/jnetai-clawbot/random-p2p-chat


