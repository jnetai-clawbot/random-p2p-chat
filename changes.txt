Project implementation started
Update 1.0.13
- Changed: Package name to com.p2pchat.random (separate from p2p-chat app).
- Changed: App label to "Random P2P Chat" for clear differentiation.
- Changed: HTML title and header to "Random P2P Chat".
- Changed: Unique keystore (random-p2p-chat.keystore, alias randomp2pchat).
- Changed: GitHub secrets use RANDOM_ prefix for independent builds.
- Incremented version to 1.0.13 (v13).

Update 1.0.12
- Added: Voice note recording (hold mic button to record, release to send via WebRTC data channel).
- Added: Voice call (full duplex audio call between paired users using WebRTC media).
- Added: Video call (full duplex audio+video call with front camera, picture-in-picture local preview).
- Added: Call controls: mute mic, toggle camera, toggle speaker, hang up.
- Added: Incoming call auto-answer with audio/video stream setup.
- Added: RECORD_AUDIO and MODIFY_AUDIO_SETTINGS permissions to AndroidManifest.
- Added: WebView permission request handler for camera/mic access.
- Added: Voice note playback button on received voice messages.
- Added: Call overlay UI with remote+local video, call controls, status text.
- Added: Voice note recording indicator with timer and pulse animation.
- Incremented version to 1.0.12 (v12).

Update 1.0.11
- Added: Random user pairing via matchmaking WebSocket server (server/index.js).
- Added: "Connect with Random User" button that searches for other app users.
- Added: Block user button (blocks and searches for next user).
- Added: Skip user button (finds another user, deprioritizes skipped user).
- Added: Search status indicator with spinner animation.
- Added: Cancel search button to stop random pairing search.
- Added: Matchmaking server with heartbeat, cleanup, blocked/skipped pair tracking.
- Added: GitHub Actions workflow (.github/workflows/release.yml) for APK builds.
- Added: .gitignore file.
- Updated: GitHub repo URL to random-p2p-chat.
- Updated: End Chat now properly notifies matchmaking server.
- Incremented version to 1.0.11 (v11).

Update 1.0.10
- Renamed release APK to p2p-chat.apk
- Updated version to 1.0.10

Update 1.0.9
- Added: Settings persistence via localStorage (settings survive app restarts).
- Added: Empty save folder now saves files directly to Downloads root (no subfolder).
- Added: Screen lock prevention enabled by default with toggle in Settings.
- Incremented version to 1.0.9 (v9).

Update 1.0.8
- Fixed: File visibility issues in Downloads folder by correctly using MediaStore's IS_PENDING flag.
- Added: Global P2P Support via OpenRelay public TURN servers (works across different networks).
- Added: Dual IPv4 and IPv6 support for WebRTC connections.
- Added: "Connection Info" section in Settings showing Public IP and WebRTC status.
- Added: "Enable TURN" toggle in Settings to allow global connectivity.
- Incremented version to 1.0.8 (v8).

Update 1.0.7
- Fixed: Received files now correctly save to the public "Downloads/P2PChat" folder.
- Added: Settings Menu with toggles for Auto Reconnect, File Transfers, and Nudge Vibration.
- Added: "End Chat" button to gracefully disconnect from a peer.
- Added: Auto-reconnect logic that attempts to restore lost connections.
- Incremented version to 1.0.7 (v7).

Update 1.0.6
- Added: "About" section with creator info and version display.
- Added: "Check for Update" button linking to latest GitHub release.
- Added: "Share App" button to easily share the download link.
- Enhanced: QR Scanner now correctly shows camera preview and supports scanning from Gallery.
- Incremented version to 1.0.6 (v6).

Update 1.0.5
- Added: Randomized unique Peer IDs (Pins) instead of static device IDs.
- Added: QR Code generation for the local ID to simplify pairing.
- Added: "Get New ID / QR" button to regenerate the connection Pin.
- Fixed: UI layout issues where the Connect button was pushed out of view.
- Incremented version to 1.0.5 (v5).

Update 1.0.4
- Fixed: Web page not available error by correcting WebViewAssetLoader path mapping and URL.
- Incremented version to 1.0.4 (v4).

Update 1.0.3
- Fixed: Web page not available error by adding missing local web assets.
- Fixed: App now operates entirely offline for assets, using WebViewAssetLoader.
- Added: PeerJS for P2P communication via STUN servers.
- Added: Signing configuration using release keystore for seamless updates.
- Incremented version to 1.0.3 (v3).

